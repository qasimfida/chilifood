import AppThemeProvider from './AppThemeProvider';
import DARK_THEME from './dark';
import THEME from './theme';

export {
    THEME as default, // Change to DARK_THEME if you want to use dark theme as default
    DARK_THEME,
    THEME,
    AppThemeProvider,
};
